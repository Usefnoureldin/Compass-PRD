VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-or-anon-key

# Server-side only — never committed, never shipped to the browser bundle.
# Required by hostbase-product/scripts/sync-to-compass.ts once Compass RLS
# is tightened. Grab from Supabase Dashboard → Settings → API → service_role.
SUPABASE_SERVICE_ROLE_KEY=
